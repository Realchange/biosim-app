{
  "name": "biosim",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "BIOSIM monorepo: @biosim/core (headless scientific engine + Hypothesis Engine) and @biosim/app (Vite/React frontend).",
  "workspaces": [
    "core",
    "app"
  ],
  "scripts": {
    "test": "npm run test -w @biosim/core",
    "test:core": "npm run test -w @biosim/core",
    "dev": "npm run dev -w @biosim/app",
    "build": "npm run build -w @biosim/core && npm run build -w @biosim/app",
    "lint": "npm run lint -w @biosim/app"
  }
}
